/**
 * Network Components Index
 * 
 * Re-exports all network-related components for easy importing
 */

// Context and hooks
export { NetworkProvider, useNetwork } from '../../contexts/NetworkContext';

// UI Components
export { NetworkSelector } from '../NetworkSelector';
export { NetworkBadge, NetworkDot, NetworkIndicator } from '../NetworkBadge';
export { TestnetWarningBanner, TestnetLabel, TestnetTransactionWarning } from '../TestnetWarningBanner';

// Types
export { NetworkType, NETWORK_CONFIGS } from '../../types/network';
export type { NetworkConfig, NetworkContextType } from '../../types/network';

// Utilities
export {
  getStacksNetwork,
  saveNetworkPreference,
  loadNetworkPreference,
  clearNetworkPreference,
  getContractAddress,
  isValidNetwork,
  getNetworkFromURL,
} from '../../utils/networkUtils';
